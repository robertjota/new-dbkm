<?php View::flash(); ?>

<!-- Page Content -->
<div class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="block pt-3">
                <div class="block-header pt-3 d-flex justify-content-center">
                    <?php echo DwHtml::button("sistema/configuracion/", 'basic', array('class' => 'mx-1 btn-sm btn-info'), 'fa-asterisk', APP_AJAX); ?>
                    <?php echo DwHtml::button("sistema/configuracion/empresa/", 'empresa', array('class' => 'mx-1 btn-sm btn-success active'), 'fa-building', APP_AJAX); ?>
                    <?php echo DwHtml::button("sistema/configuracion/config/", 'config all', array('class' => 'mx-1 btn-sm btn-info'), 'fa-cog', APP_AJAX); ?>
                    <?php echo DwHtml::button("sistema/configuracion/routes/", 'routes', array('class' => 'mx-1 btn-sm btn-info'), 'fa-random', APP_AJAX); ?>
                    <?php echo DwHtml::button("sistema/configuracion/databases/", 'databases', array('class' => 'mx-1 btn-sm btn-info'), 'fa-hdd-o', APP_AJAX); ?>
                </div>
                <div class="block-content">

                    <pre><?php // print_r($empresa); ?></pre>

                    <form action="" method="post" enctype="multipart/form-data" class="no-ajax">

                    <div class="row justify-content-evenly">

                        <div class="col-md-5">
                            <fieldset class="border px-3">
                                <legend class="float-none w-auto block-header block-header-default">Datos de la Empresa</legend>
                                <div class="mb-3">
                                    <label class="form-label">Nombre de la empresa</label>
                                    <input type="text" name="empresa[nombre]" value="<?php echo $empresa['nombre']; ?>" class="form-control">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">RIF</label>
                                    <input type="text" name="empresa[rif]" value="<?php echo $empresa['rif']; ?>" class="form-control">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Dirección</label>
                                    <input type="text" name="empresa[direccion]" value="<?php echo $empresa['direccion']; ?>" class="form-control">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Teléfono</label>
                                    <input type="text" name="empresa[telefono]" value="<?php echo $empresa['telefono']; ?>" class="form-control">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Correo electrónico</label>
                                    <input type="email" name="empresa[email]" value="<?php echo $empresa['email']; ?>" class="form-control">
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Sitio web</label>
                                    <input type="text" name="empresa[web]" value="<?php echo $empresa['web']; ?>" class="form-control">
                                </div>
                            </fieldset>
                        </div>

                        <div class="col-md-5">
                            <fieldset class="border px-3">
                                <legend class="float-none w-auto block-header block-header-default">Logos de la Empresa</legend>
                                
                                <div class="mb-3">
                                    <label class="form-label">Logo Principal (para sidebar)</label>
                                    <?php 
                                    $logoExtensions = ['png', 'jpg', 'jpeg', 'svg'];
                                    $logoExists = false;
                                    foreach($logoExtensions as $ext) {
                                        if(file_exists(PUBLIC_PATH . 'empresa/logo-empresa.' . $ext)) {
                                            echo '<div class="mb-2"><img src="'.PUBLIC_PATH.'empresa/logo-empresa.'.$ext.'" alt="Logo" class="img-thumbnail" style="max-height: 80px;"></div>';
                                            $logoExists = true;
                                            break;
                                        }
                                    }
                                    if(!$logoExists) echo '<div class="mb-2 text-muted">No hay logo cargado</div>'; 
                                    ?>
                                    <input type="file" name="logo" class="form-control" accept="image/png,image/jpeg,image/gif,image/svg+xml">
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label">Logo Mini (sidebar colapsado)</label>
                                    <?php 
                                    $logoMiniExists = false;
                                    foreach($logoExtensions as $ext) {
                                        if(file_exists(PUBLIC_PATH . 'empresa/logo-mini.' . $ext)) {
                                            echo '<div class="mb-2"><img src="'.PUBLIC_PATH.'empresa/logo-mini.'.$ext.'" alt="Logo Mini" class="img-thumbnail" style="max-height: 40px;"></div>';
                                            $logoMiniExists = true;
                                            break;
                                        }
                                    }
                                    if(!$logoMiniExists) echo '<div class="mb-2 text-muted">No hay logo mini cargado</div>'; 
                                    ?>
                                    <input type="file" name="logo_mini" class="form-control" accept="image/png,image/jpeg,image/gif,image/svg+xml">
                                </div>

                            </fieldset>
                        </div>
                    </div>
                    <hr>
                    <div class="row mb-3 text-end">
                        <div class="form-actions">
                            <button type="submit" class="btn btn-success">GUARDAR DATOS DE EMPRESA</button>
                        </div>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>